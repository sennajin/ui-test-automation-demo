# Store Configuration
STORE_URL=https://prometheamosaic.com/

# Browser Settings
HEADLESS=true
TIMEOUT=30000

# Evidence Collection
CAPTURE_SCREENSHOT=only-on-failure
CAPTURE_VIDEO=retain-on-failure

# Test Configuration
RETRIES=1
ANCHOR_COLLECTION_HANDLE=bookmarks

# CI/CD (set by GitHub Actions)
CI=false
